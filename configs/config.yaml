data:
  train_path: "dataset_processed/CarbPI-site/test/"
  valid_path: "dataset_processed/CarbPI-site/valid/"  
  test_path: "dataset_processed/CarbPI-site/test/"
  window_size: 0
  local_dim: 2560
  protein_dim: 2560

model:
  protein_dim: 2560
  local_dim: 2560
  hidden_dim: 128
  n_layers: 3
  n_heads: 8
  pf_dim: 256
  dropout: 0.1
  kernel_size: 7

training:
  batch_size: 32
  learning_rate: 0.0001
  weight_decay: 0.0001
  epochs: 30
  early_stopping: 10
  decay_interval: 10
  lr_decay: 0.9
  seed: 42

paths:
  output_dir: "outputs/"
  model_dir: "models/"
  result_dir: "results/"
  experiment_name: "Carb-Train517-Val129-Test162"
